# =============================================================================
# Flight Tracker - Environment Variables
# =============================================================================
# Copy this file to `.env` and fill in real keys. Do NOT commit .env to git.

# --- Server ---
PORT=3001
NODE_ENV=development

# --- 3rd Party APIs (Backend will call these; client never sees the keys) ---
# TODO: BACKEND - Sign up and add keys
FLIGHTRADAR24_API_KEY=
WEATHER_API_KEY=

# --- Optional: DB connection if we add persistent storage ---
# DATABASE_URL=
