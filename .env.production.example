# Production environment for the React build.
# This file IS committed (no secrets here -- the URL is public anyway).
#
# Set this to the URL of your deployed backend (e.g. on Render).
# In dev, leave this unset -- Vite proxies /api/* to localhost:3001.
VITE_API_BASE=https://flight-tracker-api.onrender.com
